services:
  - type: web
    name: shortener-web
    runtime: static
    buildCommand: cd web && pnpm install --frozen-lockfile && pnpm run build
    staticPublishPath: ./web/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: shortener-api
          type: web
          envVarKey: RENDER_EXTERNAL_URL

  - type: web
    name: shortener-api
    plan: free
    runtime: node
    region: singapore
    buildCommand: cd server && pnpm install --frozen-lockfile && pnpm run build
    startCommand: cd server && pnpm run db:migrate && pnpm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: shortener-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: shortener-cache
          type: keyvalue
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
    healthCheckPath: /health

  - type: keyvalue
    name: shortener-cache
    plan: free
    region: singapore
    ipAllowList: []

databases:
  - name: shortener-db
    databaseName: shortener
    plan: free
    region: singapore
    ipAllowList: []
