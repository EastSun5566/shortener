# Development Dockerfile for web service
# This is separate from main Dockerfile to support hot reload in development

FROM node:24-alpine

# Enable the package manager pinned in package.json
RUN corepack enable

WORKDIR /app

# Copy workspace config first for better layer caching
COPY pnpm-workspace.yaml package.json pnpm-lock.yaml ./

# Copy web package files
COPY web/package.json ./web/

# Install all dependencies (including dev dependencies)
RUN pnpm install --frozen-lockfile --filter web

# The source code will be mounted as a volume in docker-compose.yml
# This allows hot reload during development

EXPOSE 3001

# Default command (can be overridden in docker-compose.yml)
CMD ["sh", "-c", "cd /app/web && pnpm run dev"]
